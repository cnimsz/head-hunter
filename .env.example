# Copy to .env.local for local dev, and set the same value as a Vercel env var for prod.
# MUST match the HEAD_HUNTER_APP_TOKEN Supabase secret set on the edge function.
# Generate with: openssl rand -hex 32
VITE_HH_APP_TOKEN=
